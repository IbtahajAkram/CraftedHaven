import type { Metadata } from 'next'
import { GeistSans } from 'geist/font/sans'
import { GeistMono } from 'geist/font/mono'
import './globals.css'

export const metadata: Metadata = {
  title: 'CraftedHaven',
  description: 'CraftedHaven – Transforming homes with premium, handcrafted furniture that combines timeless design, exceptional quality, and sustainable craftsmanship. Discover elegant sofas, tables, beds, and décor to elevate your living spaces. Created by Muhammad Zawwar Akram.',
  generator: 'Muhammad Zawwar Akram',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <head>
        <style>{`
html {
  font-family: ${GeistSans.style.fontFamily};
  --font-sans: ${GeistSans.variable};
  --font-mono: ${GeistMono.variable};
}
        `}</style>
      </head>
      <body>{children}</body>
    </html>
  )
}
